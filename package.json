{
  "name": "briend",
  "version": "1.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:web": "bun run --cwd packages/web dev",
    "dev:app": "bun run --cwd packages/web dev & bun run --cwd packages/container start",
    "android:container": "bun run --cwd packages/container android",
    "ios:container": "bun run --cwd packages/container ios",
    "clean": "rm -rf node_modules && rm -rf packages/*/node_modules && bun install"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.8.2"
  }
}
